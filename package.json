{
  "name": "courtclimate",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "cd server && npm ci && npm run build",
    "start": "cd server && npm run start"
  }
}
