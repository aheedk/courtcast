# Google Maps JavaScript API key (Places API enabled, restricted to
# http://localhost:5173 in dev).
VITE_GOOGLE_MAPS_KEY=

# Same OAuth Client ID as the server's GOOGLE_OAUTH_CLIENT_ID.
VITE_GOOGLE_OAUTH_CLIENT_ID=

# Native iOS Sign in with Apple. Defaults match the checked-in iOS bundle id.
VITE_APPLE_CLIENT_ID=com.courtclimate.app
VITE_APPLE_REDIRECT_URI=https://courtclimate.com/login

# Optional. Leave empty for same-origin web builds. Set to
# https://courtclimate.com for bundled native builds that call prod API.
VITE_API_BASE_URL=

# Where to center the map if geolocation is denied. Defaults to NYC.
VITE_DEFAULT_LAT=40.7831
VITE_DEFAULT_LNG=-73.9712
